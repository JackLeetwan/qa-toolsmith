# Environment configuration for E2E tests with cloud Supabase
# Copy this file to .env.test and fill in your actual values

# Supabase Cloud Project for E2E Testing
# Get these values from: https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your-anon-key-here

# Optional: service role key for admin cleanup (teardown removes test users)
# Required if you want E2E teardown to delete dynamically created test accounts
# DO NOT use service role key outside CI or secured environments
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# E2E Test User Credentials
# Create a test user in your cloud Supabase project
# Dashboard -> Authentication -> Users -> Add user
E2E_USERNAME_ID=user-uuid-from-supabase-auth
E2E_USERNAME=test-user@example.com
E2E_PASSWORD=your-test-user-password

# Notes:
# 1. Never commit .env.test to version control (already in .gitignore)
# 2. Use a dedicated Supabase project for E2E tests (not production)
# 3. The test user should have appropriate permissions for test scenarios
# 4. Database schema should be migrated using: supabase db push

